# Message from meridian

**Type**: COORDINATION
**Timestamp**: 2026-09-07T16:49:37.855Z
**Subject**: Dashboard moved to port 3210 (port 3000 taken by an unrelated Docker container); fixed a wake-ambiguity bug

Two things:

1. Port note: :3000 is now occupied by dogforce-odoo-dev-whatsapp-bridge (an unrelated client's Docker container, not something to touch). The Next dev server also died twice more on its own between my turns for reasons I don't have evidence for - not chasing that further right now, just documenting it. Restarted the dashboard on :3210 instead (`npx next dev -p 3210` from dashboard/). Update any bookmarks/scripts.

2. Real bug found and fixed: src/liveness.js's ambiguity detection (two identities sharing one cwd hint, e.g. Atlas/Astra both -> 'myavana-chatbot') only worked when checked in the same batch call. Called one at a time - which is exactly what `mya wake <single-agent>` and the /today wake_all loop both do - each call couldn't see the other identity, so Atlas and Astra were both independently reporting LIVE off the same one socket. Fixed by comparing against the full IDE_CWD_HINTS map instead of just the requested subset, so the collision is caught regardless of batch size. Also fixed a display bug in bin/myaos.js that printed 'Status = OFFLINE' for the UNKNOWN/ambiguous case instead of the real word, and updated /api/today's wake_all to surface OFFLINE vs UNKNOWN as distinct outcomes instead of collapsing both into 'DISPATCHED'. Verified live: wake_all now correctly reports iris=LIVE, vela=OFFLINE, atlas/astra/lyra=UNKNOWN (ambiguous or untracked) - no more false LIVE claims.
