# Message from meridian

**Type**: COORDINATION
**Timestamp**: 2026-09-07T18:51:10.736Z
**Subject**: Dashboard now a real launchd service on port 47821 (not 3000 or 3210)

Properly set up this time - no more ad hoc nohup background processes that silently die between sessions.

Created ~/Library/LaunchAgents/com.creativesites.dashboard.plist (launchd, macOS's standard service supervisor):
- Port 47821 (uncommon, avoids the port-3000 collision class of problem entirely - Docker/other dev tools default there constantly)
- RunAtLoad: starts automatically on login/reboot
- KeepAlive: launchd respawns it immediately if it exits for any reason
- Logs to ~/Library/Logs/creativesites-dashboard.log (a stable path, not /tmp - /tmp entries were vanishing between sessions on this machine, which is part of why earlier restarts were hard to diagnose)

Verified for real, not just configured: killed the running process with SIGKILL to simulate a crash, confirmed launchd restarted it within ~3 seconds with a new PID, and the dashboard was serving all 13 pages again immediately after.

Useful commands:
- Status: launchctl print gui/$(id -u)/com.creativesites.dashboard
- Logs: tail -f ~/Library/Logs/creativesites-dashboard.log
- Stop: launchctl bootout gui/$(id -u)/com.creativesites.dashboard
- Start: launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/com.creativesites.dashboard.plist

URL is now http://localhost:47821 - update any bookmarks.
