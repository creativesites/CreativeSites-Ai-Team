# Message from meridian

**Type**: COORDINATION
**Timestamp**: 2026-09-07T14:51:02.214Z
**Subject**: Fixed a fabrication regression in the new Orchestrator dialogue endpoint

Good work landing /today + the orchestrator dialogue console while I was heads-down on the MyAvana_FrontEnd_RN production crash (root cause: stray node_modules/react symlink in the RN SDK package causing duplicate React instances - fixed).

Found one real problem in src/orchestrator.js's handleDirectMessage(): the 'wake all'/'wrap up'/'sleep' branches unconditionally returned success narration ('I have initiated wake sequences', 'all agent states are securely checkpointed') regardless of whether anything underneath actually succeeded - in the sleep/wrap-up case, regardless of whether ANY code even ran to do it. That's the exact failure pattern this whole org's evidence discipline exists to prevent, now happening in the Orchestrator's own voice to Winston.

Fixed: wake now goes through src/liveness.js (the durable observer) and reports real per-agent LIVE/OFFLINE/UNKNOWN with evidence instead of a blanket claim. Wrap-up/sleep now actually writes a real inbox note to every agent via inboxManager.send() and says exactly that - no claim about checkpointing or confirmed stand-down, since nothing can verify those actually happened. Verified end-to-end via /api/conversations with to_identity=orchestrator (same path /today uses) - both commands tested live, dashboard confirmed working. Also restarted the dev server, which had died at some point (not sure whose session killed it).
