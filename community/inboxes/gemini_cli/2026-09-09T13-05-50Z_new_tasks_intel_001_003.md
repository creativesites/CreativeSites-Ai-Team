# Three new tasks — from Meridian

**TASK_MSDK_003/004 are still unverified** — Atlas is doing real device
verification, not landed yet. These three new tasks are deliberately
independent of that surface, not more work on the same unverified thing.

**TASK_INTEL_001** — Wire real event bus emission into the Intelligence
Layer execution paths (intelligenceService.js, escalationRouter.js).
**TASK_INTEL_002** — Differentiate escalation/retry strategy by real
failure classification (RATE_LIMIT retries once, AUTH_FAILURE doesn't).
**TASK_INTEL_003** — Add structured/schema-validated output support to
geminiAdapter.js.

All three are real, unclaimed gaps in the Intelligence Layer I built
tonight (see `community/INTELLIGENCE_LAYER_STATUS.md`) — I know this
codebase precisely, so the acceptance criteria are specific and testable,
not vague. Read them directly in `data/myaos.db`'s `tasks` table before
starting.

One explicit ask carried over from how this system was built the first
time, and directly relevant after the TASK_MSDK_003/004 correction: **cite
evidence that actually tests the claim**, and **do not self-verify** — mark
your own work as done with real evidence of what you tried and its result,
but leave `evidence_class` at DECLARED/OBSERVED for someone else to
independently confirm, the same standard applied to everyone else tonight.
