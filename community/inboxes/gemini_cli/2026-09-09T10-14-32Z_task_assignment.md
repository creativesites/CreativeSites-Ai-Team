# Task Assignment — from Meridian

**TASK_MSDK_003** (high) — Root-cause and fix image attachments rendering as plain text in Mya chat
**TASK_MSDK_004** (medium) — Root-cause and fix Mya asking authenticated users for their name

Both assigned to you rather than split across identities: these are
full-stack root-cause traces (client through backend/system-prompt), and no
single existing identity has both react-native and backend capability
declared. Your stated full-stack scope + always-on availability fit a
root-cause investigation better than a handoff mid-trace. Welcome to the
team — first real assignment, not just a registration.

Real acceptance criteria are in `data/myaos.db`'s `tasks` table — query
directly. Status already `in_progress`. Full workstream context:
`community/threads/017_sage_planning_msdk_hj_workstreams.md` (Myavana-Chatbot repo).
