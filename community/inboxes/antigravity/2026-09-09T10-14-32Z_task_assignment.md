# Task Assignment — from Meridian

**TASK_HJ_019** (urgent) — Re-verify remaining open P0s (empty nav, network-idle timeout)

Assigned to you: only identity with playwright/browser-automation capability
declared, and this specifically needs Level 3 browser evidence, not curl.
I already narrowed scope with Level 2 checks (see facts FACT-012/FACT-013 in
data/myaos.db) — nav menu confirmed still empty, chat-token 403->401 change
observed but cause not confirmed, Kommunicate widget still embedded as a
plausible P0-4 cause. None of that closes the task on its own; it's a
starting point so you're not re-deriving from scratch.

If root-causing requires actual WordPress admin/plugin changes once
observed (not just verification), loop in Vela rather than expanding scope
yourself. Status already `in_progress`.
